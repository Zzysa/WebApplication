{
    "name": "account-service",
    "version": "1.0.0",
    "description": "Backend service for the project",
    "main": "server.js",
    "scripts": {
        "start": "npx prisma migrate deploy && node server.js",
        "dev": "npx prisma generate && npx prisma migrate deploy && nodemon server.js",
        "seed": "npx prisma db seed"
    },
    "prisma": {
        "seed": "node prisma/seed.js"
    },
    "dependencies": {
        "@prisma/client": "^6.9.0",
        "cors": "^2.8.5",
        "express": "^4.19.2",
        "jsonwebtoken": "^9.0.0",
        "express-validator": "^7.2.1",
        "firebase-admin": "^13.4.0",
        "helmet": "^8.1.0"
    },
    "devDependencies": {
        "nodemon": "^3.1.4",
        "prisma": "^6.9.0"
    }
}